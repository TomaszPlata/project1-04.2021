alert('Click that');

const user_input = true;

if (user_input==true) {
    document.getElementById('attention').innerHTML = "Everything's gonna be fine, Jesus is with you^^!";
} else {
    document.getElementById('attention').innerHTML = "You need to rewrite your code";
}
